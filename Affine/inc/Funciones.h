#include <string>
#include <fstream>
#include <iostream>
#include <vector>
#include <time.h>
using namespace std;

string readFile();
void writeFile(string);
int mod(int, int);
int mcd(int, int);
vector<int> mcdE(int, int);